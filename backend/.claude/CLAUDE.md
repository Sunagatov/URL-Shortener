Use `../../AGENTS.md`.

This backend-local Claude file is intentionally a thin pointer. The root
`AGENTS.md` is the canonical source for current commands, architecture routing,
and the Vault boundary.
